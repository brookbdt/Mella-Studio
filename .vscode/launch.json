{
    "version": "0.2.0",
    "configurations": [
        {
            "command": "pnpm dev",
            "name": "Run Dev Server",
            "request": "launch",
            "type": "node-terminal",
            "env": {
                "EDITOR": "code"
            }
        }
    ]
}
